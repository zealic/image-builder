include:
  - remote: https://arp.to/@ci/docker.yml

distro-debian:
  stage: build
  tags:
    - nbd
  only:
    - master
  script:
    - apk add findutils
    - make debian:build
    - make debian:artifacts
